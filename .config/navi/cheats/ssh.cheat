% ssh
@ envs

# local port forwarding: local → remote server 
ssh -fN -L <local_port>:<remote_host>:<remote_port> <remote_host>

# remote port forwarding: remote server → local
ssh -fN -R <remote_port>:<local_host>:<local_port> <remote_host>

