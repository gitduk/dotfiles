[Unit]
Description = Hugo Service
After = network.target syslog.target

[Service]
Type = simple
WorkingDirectory = %h/.hugo/
ExecStart = %h/.linuxbrew/bin/hugo server
Restart = always
RestartSec = 5s

[Install]
WantedBy = default.target

