Search for and install agent skills from the open skills ecosystem.

1. Run `bun x skills find $ARGUMENTS` to search
2. Present results with skill name, description, and install command
3. If the user wants to install, run: `bun x skills add <owner/repo@skill> -g -y --copy`

Browse all skills at: https://skills.sh/
